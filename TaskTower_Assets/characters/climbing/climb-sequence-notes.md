# Climb sequence

All frames use `viewBox="0 0 160 240"`.
Render at one fixed CSS size and advance frames every 110–140 ms while easing the character container upward over 650 ms.
For `prefers-reduced-motion`, display frame 05 without animation.