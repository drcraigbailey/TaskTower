# Tower component guide

The complete tower uses `viewBox="0 0 360 720"`; floor modules use `0 0 180 110`.
Repeat left and right floor modules behind the centre divider for configurable heights, then add one base and roof.
Position a player marker with `bottom = 8% + (floors / towerHeight) * 82%`.
Use the zoom section as a clipped viewport around nearby players.