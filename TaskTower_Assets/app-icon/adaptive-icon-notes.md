# Adaptive icon notes

- Foreground safe zone: central 66% of the 432×432 viewBox.
- Background is full bleed.
- Android package: `app.tasktower.home`.
- Use `icon-1024.png` as the Capacitor source, or the separate foreground/background SVGs in an adaptive-icon pipeline.
- Never add the tagline to launcher icons.